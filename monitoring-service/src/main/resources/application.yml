server:
  port: 8083

spring:
  application:
    name: monitoring-service

  datasource:
    url: jdbc:postgresql://postgres:5432/rmt-platform?currentSchema=schema_monitoring
    username: ${SPRING_DATASOURCE_USERNAME:rmt_admin}
    password: ${SPRING_DATASOURCE_PASSWORD:change_me}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50
        order_inserts: true
        default_schema: schema_monitoring

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
    default-schema: schema_monitoring
    liquibase-schema: schema_monitoring
    drop-first: false

  # Кеширование для DTO агрегатов (чтобы не дёргать другой сервис каждые 5 сек)
  cache:
    type: caffeine
    cache-names: aggregates
    caffeine:
      spec: maximumSize=100,expireAfterWrite=10m

# Modbus: только ЧТЕНИЕ телеметрии (не управление!)
modbus:
  host: ${MODBUS_HOST:192.168.1.100}
  port: ${MODBUS_PORT:502}
  polling-interval: 5000
  read-timeout: 3000
  connection-timeout: 5000
  retry-attempts: 3

# Feign: вызовы к aggregate-control-service за инфо об агрегатах
feign:
  client:
    config:
      aggregate-control-service:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

services:
  aggregate:
    url: ${SERVICES_AGGREGATE_URL:http://aggregate-control-service:8082}

# Grafana: read-only пользователь для прямого подключения к БД
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
        step: 1m

logging:
  level:
    root: INFO
    ru.practicum.monitoring: DEBUG
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN