spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rmt-platform?currentSchema=schema_monitoring
    username: rmt_admin
    password: change_me

  jpa:
    hibernate:
      ddl-auto: update  # Для локальной разработки автосоздание таблиц
    show-sql: true

logging:
  level:
    root: INFO
    ru.practicum.monitoring: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

modbus:
  host: localhost
  port: 502